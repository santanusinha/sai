agentId: test-agent
name: Test Agent
description: A test persona with env var substitution
model: ${TEST_MODEL_PROVIDER}/gpt-4o
prompt: |
  You are a ${TEST_AGENT_ROLE} assistant working in ${TEST_WORKSPACE_DIR}.
